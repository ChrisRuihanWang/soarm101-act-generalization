# 当前复现状态

本次只整理现有代码和实验记录，没有执行硬件采集、重新训练或重新评估策略。

## 已知限制

- 采集与分析脚本使用固定本机路径、设备和数据集标识，尚未统一参数化。
- V1 脚本引用的 `data/fixed_v1_30ep` 不在当前项目根目录的数据集中；保留为历史脚本。
- `full_quality_check_grid5_t1.py` 的 `EXPECTED_EPISODES` 仍为 15，但最终数据集元信息记录 50 条。用于最终数据检查时需要调整。
- 三个完整质检脚本共用 `logs/quality_episode_summary.csv` 和 `logs/quality_tracking.csv`，后运行的检查会覆盖前一次输出。
- `diagnose_transport_cap10_full1.py` 仍使用 `CLAMP=3.0`。尚未确认这是对照阈值还是历史遗留设置，不能将输出视为 10.0 阈值的统计。
- `play_grid5_episode.py` 能定位分片视频，但 episode 跨视频边界时只播放当前文件内片段并给出提示。
- `check_rollout_*` 和 `diagnose_transport_*` 是离线分析脚本，不是实时控制保护机制或自动任务成功判定器。

## 待补充的证据

- Stage2/Stage3/压力测试/网格复测的逐次结果标注与视频对应关系。
- 网格尺寸、连续位置采样规则、已见/未见定义和统一成功标准。
- Stage3 混合数据源及 episode 映射、去重与合并过程。
- 各评估使用的检查点、动作步数、时间集成、限幅、时长及人工干预记录。
- 原始 LeRobot editable 源码提交号及本地修改，训练硬件与完整环境信息。

Stage1 摘要中限幅从 3.0 调整到 10.0 后恢复跟踪的描述来自既有实验记录；未在此次整理中做受控对照验证。
