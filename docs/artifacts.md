# 数据与模型

原始 Parquet 数据、摄像头录像、模型权重及优化器状态保留在本地，没有包含在本仓库中。下载链接、访问方式及校验值待发布；在此之前不能仅靠克隆仓库复现训练与录像分析。

| 数据集 | 元信息中的 episode 数 | 元信息中的帧数 |
|---|---:|---:|
| `fixed_v2_standardized_30ep` | 30 | 14,355 |
| `grid5_t1_50ep` | 50 | 23,295 |
| `act_stage23_100ep` | 100 | 40,342 |

上述数据元信息记录格式版本 `v3.0`、30 FPS。格式版本不是 LeRobot 包的版本号。

模型对应关系见 [训练说明](training.md)。未来发布用于推理的模型时，应提供完整 `pretrained_model/`，包括模型权重、预处理/后处理配置和归一化参数。恢复训练还需要相应 `training_state/`。
